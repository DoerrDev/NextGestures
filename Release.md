## 首个正式版本 🎉

NextGestures 是一款轻量、可编程的 Windows 鼠标手势工具，灵感来自 WGesture，用来解决它在新系统上不兼容、且长期不再更新的问题。

### 功能

- 支持右键 / 中键 / 侧键 1 / 侧键 2 作为触发键，绘制任意方向轨迹，可叠加滚轮或屏幕边角触发
- 手势动作支持：发送快捷键、按键序列、打开网址（可携带选中文本）、运行程序/命令、执行 Lua 脚本
- 按前台程序分组配置，不同软件下同一手势可以对应不同动作
- 绘制手势时有实时轨迹反馈，轨迹颜色、粗细、辉光可自定义
- 自动检测相似轨迹并提示冲突，避免误触发
- 列表 / 卡片两种视图，支持按名称、动作搜索和启用状态筛选
- 系统托盘常驻，支持全局热键和托盘菜单快速启停
- 支持开机自启动
- 内置自动更新：启动时静默检测新版本，发现更新会提示日志并一键安装重启

欢迎反馈问题和建议。
